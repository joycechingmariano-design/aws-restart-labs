import os

os.system("dir")
import subprocess

subprocess.run(["cmd", "/c", "dir"])
import subprocess

subprocess.run(["cmd", "/c", "dir"])
import subprocess

import subprocess

subprocess.run(["cmd", "/c", "dir", "sys-admin.py"])
import subprocess

command = "systeminfo"

print(f"Gathering system information with command: {command}")
subprocess.run(["cmd", "/c", command])
import subprocess

command = "tasklist"

print(f"Gathering active process information with command: {command}")
subprocess.run(["cmd", "/c", command])